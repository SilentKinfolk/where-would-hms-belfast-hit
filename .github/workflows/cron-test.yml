name: Cron test

# Throwaway diagnostic: a minimum-interval schedule with zero deps. If this
# fires but Fire the guns doesn't, the bug is specific to log-impact.yml's
# workflow registration (and we recreate that file). If this one ALSO
# doesn't fire, schedules are broken at the repo/account level.
# Delete this file once we have the answer.

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "fired at $(date -u)"
