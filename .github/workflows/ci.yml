name: CI

on:
  push:
    branches: [main]
    # log-impact cron churns public/impacts.json every hour; nothing in there
    # changes code or tests, so skip CI for those commits (deploy still runs).
    paths-ignore:
      - 'public/impacts.json'
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
